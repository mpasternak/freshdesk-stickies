__pycache__/
*.pyc

# Wygenerowane widgety zawierają ścieżki konkretnej maszyny — źródłem jest
# make_widget.py. Każdy generuje własne.
widgets/*.jsx
!widgets/.gitkeep

# Klucz API / domena / cache — nigdy do repo (i tak trzymane w ~/.config).
key
domain
.config/
